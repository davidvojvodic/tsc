# Local Development Environment Variables
# Copy this to .env.local for local development

# Database Configuration
DATABASE_URL="mysql://tsc_user:tsc_password@localhost:3306/tsc_local"

# Application Settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Authentication
BETTER_AUTH_SECRET="your-local-auth-secret-change-this-in-production"

# Optional: Email Service (for local testing)
RESEND_API_KEY="your-resend-api-key"

# Optional: File Upload Service (for local testing)
UPLOADTHING_TOKEN="your-uploadthing-token"

# Optional: Controller/Camera Integration
# SECURITY WARNING: Never commit real camera credentials to Git!
# These are examples only - replace with your actual camera details
CAMERA_HOST="your-camera-ip-address"
CAMERA_PORT="4560"
CAMERA_USERNAME="your-camera-username"
CAMERA_PASSWORD="your-camera-password"